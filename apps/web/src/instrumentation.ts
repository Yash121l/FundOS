// Next.js instrumentation hook
// Sentry is not installed. To enable: pnpm --filter @fundos/web add @sentry/nextjs
// then implement Sentry.init() here with your SENTRY_DSN.

export function register() {
  // no-op until Sentry is installed
}
